# NC-World 的核心问题与核心创新

写给第一次接触本项目的读者。不用项目内部缩写。

## 我们在做什么

一个视频世界模型，要能被反复地、廉价地问同一个问题：如果机器人接下来执行这个动作，摄像头会看到什么。这个问题必须按世界发生的顺序回答，第 t 帧先于第 t+1 帧，不能偷看未来，而且内存占用不能随着片段变长而无限增长。

这是一个很强的约束，而它不是现代视频生成模型的训练条件。一个视频扩散 Transformer 在整段片段上做双向注意力，每一帧都在所有其他帧的照映下被反复细化。这样的模型能生成很好的视频，但按它被训练的方式，它答不了上面那个问题。

主流做法是先双向训练，再后训练成自回归。本项目的立场是这个做法治的是症状。我们从第一步梯度就按因果的方式训练，全部从随机初始化开始，不继承任何已发布权重。

## 两个真正的问题

**第一个是训练与推理的上下文不匹配。**

流式生成时，模型每一步依赖的那段历史有三个属性。它是模型自己上一刻生成的，不是真值。它已经被写进缓存并且永远不会再被修改，所以其中的任何错误都无法回头修正。它还处在一个特定的噪声水平上，而这个水平由采样器的流水线安排决定，不由训练分布决定。

标准视频训练给模型看的历史，恰好在这三点上都不是那个东西。它是数据集里的真值，它可以被后续的注意力改写，而它的噪声水平是训练时随机采的。

**第二个是闭环误差累积。**

模型在生成第 k 个片段时依赖的是自己生成的前 k-1 个片段。任何一点偏差都会成为下一步的输入，再被放大。这就是常说的曝光偏差在视频上的形态，而且比在文本上更严重，因为视频的每一步都在高维像素空间里。

这两个问题是同一件事的两面。误差之所以累积，是因为模型从未在训练中学过如何在自己犯过错的历史上继续正确地演化。

## 核心洞见

一句话：流式世界模型真正的条件不是历史真值，而是它自己已提交的、不可再改的、带着特定噪声的那段输出。训练与推理之间的全部差距，都可以分解到这个对象的三个坐标上，而且每一项的代价都能单独测出来。

这个说法的价值在于它可以被证伪，也确实被我们自己的实验反复检验过。它把一个含糊的方向，也就是让训练更像推理，变成了三个具体的、可以逐个做消融的对齐动作。

## 三项算法创新

### 一，让信息真的只能单向流动

这是最基本的一项，也是我们最强的实验结果所在。

近年有一类方法给同一段视频里的不同片段分配彼此独立的噪声强度，而不是整段共用一个。这样做的用意很清楚：让模型学会在一个已经比较干净的过去的条件下，去噪一个还很脏的未来，这正是流式推理每一刻的处境。

我们发现，这个用意在双向注意力上根本无法实现。因为双向注意力允许干净片段和噪声片段互相看见，那个本该被学会的不对称性从来没有发生过。损失函数在教模型一件它在推理时永远遇不到的事。

实验是单变量的，五臂全部从随机初始化、同一批数据、同样 7500 步、同一评测协议。双向骨干加上独立噪声是 FVD 4454，而同一个双向骨干退回整段共用一个噪声反而是 1566。也就是说，这项本意在于赋予流式能力的技术，加上去比不加还差 2.8 倍。把注意力换成块状因果之后，同样的配方、同样的分块自回归评测，得到 691，比前者好 6.4 倍。

结论因此可以说得很强：因果性不是为了换取流式能力而付出的质量代价，它是让这套训练信号有意义的前提条件。

### 二，让训练时的历史真的由模型自己生成

这一项直接攻闭环误差累积。

朴素的做法是给真值历史加点噪声，假装它有误差。我们试过一个更弱的版本，从中等噪声水平只走一步去噪来伪造历史，结果那样造出来的历史比真实自回归输出干净太多，在一个十几分贝量级的问题上只买到 0.15 分贝。

现在的做法是不伪造。训练时以一定概率，让模型把紧接着上下文的那几个片段完整地自己生成出来，用推理时同一条常微分方程积分路径、同一个经过位移的时间网格、同一个块状因果掩码，第 i 个片段绝不看到第 j 大于 i 的片段。生成结果直接替换掉那几个片段的真值，然后损失只取在更靠后的片段上。于是那些片段面对的历史，就是模型自己刚吐出来的东西，带着它自己的真实缺陷。

关键的工程点是整个内部生成过程在无梯度模式下运行。历史对参数而言是常数，梯度保持普通流匹配的形式，不穿过生成过程反传。这是这件事能负担得起的原因。

### 三，让训练走的噪声阶梯就是采样器真会走的那条

流式采样器在任一时刻，把不同片段放在不同的噪声水平上，形成一道阶梯，越靠后的片段越噪。这道阶梯的形状不是任意的，它由采样器的外层步数和推进速率决定，是一个很窄的参数族。

标准做法是在所有满足非降序约束的噪声组合上均匀采样。问题是这个空间远大于推理会经过的那一族，训练的大部分预算花在了模型永远不会遇到的组合上。

我们的做法是直接从推理那一族里采样。随机抽一个相位覆盖整个扫描过程，随机抽一个带抖动的推进速率，以一定概率用这样得到的阶梯替换掉均匀采样的结果。这不是加一个正则项，是把训练的噪声分布换成推理的噪声分布。速率上的抖动是刻意的，它让模型不至于过拟合某一个特定的推进速率。

第二项和第三项一起，在受控对照下值 2.2 倍 FVD，也就是关掉之后从 283.5 退到 629.9，两臂采样设置完全相同。

## 与相关工作的区别

已有的各类强制类方法，各自修补了推理历史的一个属性。用真值历史训练的做法什么都不修。给每个片段独立噪声的做法修了噪声这一项，但历史仍然是加噪的真值。让模型在自己的生成结果上训练的做法修了自产这一项，但通常整段共享一个噪声进度，而且反传要穿过生成过程。

本项目不是再提一种强制方法。我们的主张是推理历史同时具备三个属性，任何只修其中一项的做法都留着另外两项的落差，所以我们把它们当成同一个对象的三个坐标，逐项对齐并逐项测价。

还有一条是这些工作都没有说过的：给每个片段独立噪声这件事，在双向骨干上是有害的。

## 支撑证据

以下数字全部来自本项目自己的实验，每一条都注明协议。

**因果性对比。** 从随机初始化、同数据同步数同协议、32 个片段。双向加独立噪声 FVD 4454.4，响应度 −0.006；双向加同质噪声 1566.3，响应度 0.392；块状因果加独立噪声 690.8，响应度 0.610。响应度衡量的是当场景实际运动量变为十倍时，模型输出的运动量是否也大致变为十倍，取值 1 表示忠实，取值 0 表示完全不跟随。第一臂的响应度约等于零，意味着它输出的运动量与场景的实际运动量毫无关系。

**两项对齐动作的价值。** 在长时程真机数据上，253 帧、24 个片段、采样设置逐项一致，两项都开是 FVD 283.5，都关是 629.9。

**误差累积的形状。** 253 帧的连续生成中，逐帧信噪比在前 50 帧从 28.3 分贝掉到 19.5，之后 200 帧只再掉 1.6 分贝。也就是说损失主要发生在模型第一次开始依赖自己的输出的那一刻，而不是在长时间里缓慢累积。这个观察改变了优化方向，应该攻的是最初两秒。

**编解码器不是瓶颈。** 把真值视频经同一个自编码器编码再解码，然后当作生成结果打分，21 帧处 36.21 分贝，253 帧处 35.38 分贝，只掉 0.83。自编码器的损失是空间性的，不随时长累积。我们目前最好的模型是 19.71 分贝，也就是只用掉了可用余量的 56%。

**长上下文课程的作用。** 训练窗口从 45 帧到 125 帧再到 253 帧，采样设置完全一致，FVD 从 513.9 到 350.5 到 296.0，单调改善 42%，而逐帧信噪比基本持平。课程买到的是分布层面的长时程一致性，不是逐帧锐度。

**一个反直觉的采样器发现。** 同时在流水线里被处理的片段数，与每个片段获得的去噪更新次数，两者相乘就是通常所说的采样步数。但它们方向相反。在其他条件完全相同时，把在处理的片段数从 3 提到 8，FVD 从 296.0 崩到 724.5。原因回到同一条主线：加宽流水线等于让一个片段在被提交的那一刻，它所依赖的邻居更噪。改变的是那段历史的噪声水平，不是算力总量。

**提交粒度存在内部最优。** 一次因果提交所覆盖的物理时间长度，从 0.27 秒放到 1.07 秒，响应度从 0.46 升到 0.89，但保真度代价是 FVD 增加 23%。而这个粒度恰好等于我们独立测出的动作到画面变化的延迟下限。也就是说，历史变得不可撤销的那个时间量子，同时决定了可控性的上限和质量的最优点。

## 外部基准

在公开评测协议下，官方验证划分与训练零重叠，8 帧动作条件续写，512 个片段，同一套度量实现。需要强调实现一致这件事，因为同名的 FVD 在不同实现之间可以相差三到四倍。

Bridge-V2 上我们是 45.08，两个公开基线分别是 91.6 和 101.2，冻结画面的基线是 238.7。Language-Table 上我们是 37.97，两个公开基线是 60.05 和 39.09，冻结基线 167.3。RT-1 是唯一的负结果，我们是 79.47，公开基线 68.72，我们报的是训练终点而不是事后挑出来的最优检查点。

有一处必须讲清楚。在 Bridge-V2 上，冻结画面这个什么都不做的基线，其结构相似度是 0.8821，高于我们的 0.8754。一个全程输出静止画面的基线在这个指标上赢过了我们，这说明结构相似度在该数据域该时长下不是一个可以用来支撑动态性主张的指标。我们保留这一行而不是删掉它。

## 一条评测纪律

上面那件事推出了本项目的一条硬规则。一个指标只有在什么都不做和乱动这两个基线都拿不到好分数的前提下，才配用来支撑关于运动正确性的主张。按这条规则检验，某个常用视频基准的九个维度没有一个可用，其中四个被冻结画面这个基线拿到满分，另外五个被随机打乱帧序的基线追平。

同一条纪律还有一个更隐蔽的形态。基于光流的运动指标会把画面崩解打成高分，因为画面解体本身会产生极大的光流。我们曾据此选出一批看起来运动最忠实的片段，逐帧看下来它们全部在第 40 帧崩解成抽象色块。指标与失效模式指向同一个方向，纯按指标挑选必然收敛到最坏的样本。所以任何以运动类指标为主要判据的选择，都必须先过一遍抽帧目视。
