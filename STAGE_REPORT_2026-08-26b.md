# NC-World 阶段报告（2026-08-26 · 四链收官版）

> 给第一次接触本项目的读者。全部数字均为实测，出处见
> `docs/PAPER_ROADMAP_2026-08-25.md` §7 执行日志。

---

## 1 · 这是什么项目

探索**如何从随机初始化（from scratch）预训练一个 native causal video world model**——
一个按时间顺序、逐块（chunk-by-chunk）生成未来画面的视频世界模型。它天生支持
流式生成（streaming）、动作条件 rollout、在线重规划与 planning，而非像主流视频
扩散模型那样一次性双向生成整段再切开用。

论文定位是**探索性 recipe paper**：把设计空间拆成单变量受控实验，逐个回答
"哪个设计选择买到了什么能力"。MiniWorld 是本组提前放出的论文，其架构与系统能力
面板直接作为本项目基座。

**当前中心论点**：因果注意力不是若干配料之一，而是让 diffusion forcing **可用的前提**
（从零对从零 2–6× FVD）；而"世界模型感"里最难的一块——输出的运动量是否跟随世界
实际的运动（我们称 **responsiveness**）——是所有比值型指标看不见的，必须用本项目
自造的两把非比值尺子来测。

---

## 2 · 算法与训练设置

**架构**：0.5B block-causal DiT（28 层 / 宽 1152）。视频经**冻结 Wan2.2 VAE**
压到潜空间（时间 4×、空间 16×、48 通道）；注意力块因果——每个 latent chunk
（2 潜帧 ≈ 8 RGB 帧）只看自己与过去。文本走冻结 UMT5；动作按每潜帧打包 4 帧
进入条件流。（"from scratch" 严格含义：**因果动力学骨干从随机初始化训练**，
VAE 与文本编码器是冻结的预训练组件。）

**训练目标**：rectified flow + **diffusion forcing**（窗口内各 chunk 独立噪声水平，
让模型训练时就习惯"过去清晰、未来模糊"的流式状态），叠加三项自研组件：

| 组件 | 作用 | 现状 |
|---|---|---|
| **SANS** | 训练噪声分布对齐部署采样器的实际调度 | 主线四条链全部启用 |
| **CHF** | 按概率把模型自己生成的历史喂回训练 | **域依赖**：机器人域启用；通用视频域按可采指标判负 |
| **velmatch** | 按 clip 匹配输出运动量到该 clip 真实运动量，batch 内中心化（全局水平按构造不可动）| 已实现，待训 |

**推理**：rolling KV cache 流式，每 chunk 最少 2 次前向。

**训练数据**：机器人域 DROID（真机主域）/ Bridge-V2 / Language-Table / LIBERO /
RT-1（跨本体）；通用视频 Track-B = OpenVid-HD + Ditto-1M 共 52.5 万 clip
（实测原生 20–30 fps）。评测另用 IRASim 官方发布的三数据集验证 split。

**训练配置**：muon optimizer、bf16、cosine LR、4×H200/作业、global batch 16–64。

---

## 3 · 全流程框图

```
                    ┌──────────────────────────────────────────┐
 视频(5 机器人域    │              训 练 阶 段                   │
  + 通用视频) ─────►│ 冻结 Wan2.2 VAE ──► 潜空间(48ch,4×/16×)   │
 文本 ─► 冻结 UMT5 ►│ block-causal DiT (0.5B, 28 层)            │
 动作 ─► 4帧/潜帧 ─►│  · rectified flow + diffusion forcing     │
                    │  · SANS · CHF(域依赖) · velmatch(待训)     │
                    └──────────────┬───────────────────────────┘
                                   ▼ checkpoint（内嵌 config = provenance）
                    ┌──────────────────────────────────────────┐
                    │              推 理 阶 段                   │
                    │ rolling KV cache ── 逐 chunk 流式提交      │
                    │ 真机 48.6 fps · 首帧 188 ms               │
                    │ bit-exact 分叉 · 省算 36–74% · CEM planning│
                    └──────────────┬───────────────────────────┘
                                   ▼
      ┌────────────────────────────────────────────────────────┐
      │                     评 测 体 系                          │
      │ 外部并读: Bridge/LangTable/RT-1（IRASim split、同度量）  │
      │ 通用: WMB / VBench / Physics-IQ ── 全部先过两地板        │
      │ 自造尺子: e(跨速率弹性) · b(跨场景斜率) ── 非比值、有锚点 │
      │ 纪律: 运动结论必报「水平 / 响应度 / 保真度」三件套         │
      └────────────────────────────────────────────────────────┘
```

---

## 4 · Benchmark 跑分（全部实测）

### 4.1 外部并读（同数据、同度量实现、附 do-nothing 地板）

| FVD ↓ | 冻结地板 | 本模型 | IRASim | DriftWorld |
|---|---|---|---|---|
| Bridge-V2（终版 100k）| 238.7 | **见 §7 更新** | 91.6 | 101.2 |
| Language-Table（终版 120k）| 167.3 | **37.97** | 60.05 | 39.09 |
| RT-1（30k 训练终点）| 459.5 | 79.47 | 93.0 | **68.72** |
| RT-1（10k）| — | 69.32 | — | 68.72 |

RT-1 是唯一负格，且**收敛探针改写了它的性质**：FVD 随训练**单调上升**
（10k 69.32 → 20k 76.32 → 30k 79.47）而 LPIPS 持平（0.058/0.056/0.056）——
不是预算不足，是训练后期**分布层面**退化；10k 处与 DriftWorld 仅差 0.9%。
我们仍报训练终点数（不事后挑最优点），但整条曲线一并列出。

### 4.2 绝对面板与系统能力

| 项 | 数值 |
|---|---|
| 受控双向对照（同代码同参同 clip）| 因果 PSNR 21.47 / SSIM 0.819 / LPIPS 0.110 / **FVD 229.5** vs 双向 17.73 / 0.644 / 0.240 / 1087（四项全胜）|
| LIBERO 终版 100k | 49 帧 PSNR **33.59** / LPIPS 0.0178 / FVD **56.87**；25 帧 38.13 / 93.09 |
| 流式吞吐（真机域生产配置）| **48.6 fps**（3.24× 实时），首帧 **188 ms**（双向 5.6 s）|
| 长时程 | DROID 253 帧（17 s）PSNR 19.78；LIBERO 253 帧 20.86 @57.9 fps |
| 在线重规划 | 省算 36–74%，输出 bit-exact（16/16）|
| 动作→画面结构延迟地板 | 533 ms（= 时间粒度 Δt_c，实验证实恰为质量最优点）|

### 4.3 从零对从零的因果阶梯（同批 clip，各 7500 步）

| 臂 | FVD ↓ | 响应度 b |
|---|---|---|
| 双向 + diffusion forcing | 4454.4 | **−0.006** |
| 双向 + 同质噪声 | 1566.3 | +0.392 |
| 因果 chunk1 | 699.6 | +0.456 |
| 因果 chunk4 | 852.1 | **+0.892** |
| 因果 + 预训练初始化 | 498.8 | +1.031 |

双向 + diffusion forcing 的 b≈0 意味着其输出运动量与场景实际运动量**零相关**
（条件均值的纯粹形态）。因果化后 FVD 好 2–6 倍且 b 显著更高；预训练初始化
再压 40% FVD 并把 b 推到饱和。

### 4.4 通用视频 benchmark（全部先过两地板）

| Benchmark | 冻结帧 | 乱动 | 真实视频 | 本模型 | 判决 |
|---|---|---|---|---|---|
| WorldModelBench | **9.61** | — | — | 7.85 | 冻结高于榜首 KLING 9.10 ⇒ 全榜在 do-nothing 包络内 |
| VBench subject_consistency | **1.0000** | 0.9189 | 0.9392 | 0.8809 | 冻结满分且超真实视频 |
| VBench motion_smoothness | **0.9983** | 0.8899 | 0.9861 | 0.9886 | "比真实视频还平滑"是症状不是成绩 |
| VBench dynamic_degree | 0.0000 | **0.9950** | 0.6150 | 0.2400 | 乱动近满分 ⇒ 只量"动多少" |
| VBench overall_consistency | 0.2051 | **0.2180** | 0.2180 | 0.1983 | 打乱帧序分数**逐位不变** |
| Physics-IQ | 21.0 | 1.0 | 100.0 | **1.06** | 轴可采；本模型落在乱动地板上 |

**VBench 九维经两地板法零可采**（四维被冻结帧拿满分、五维被乱动追平或超过），
加上 WMB 与 WorldScore 合成一般性主张：**现行视频世界模型评测的动态维度，
或由不动最大化，或由乱动饱和**——这正是自造 e/b 尺子的存在理由。

### 4.5 训练算力 × 推理算力面

- 加深去噪的 dynamics 代价随预训练成熟**单调收缩约 6×**（0.070→0.011），
  画质收益每步都在 ⇒ "画质–动态不可兼得"主要是**欠训练现象**；
- 推理深度有**内部最优**（4–8 步），不随训练移动；
- 按实际前向数排开，**FVD 在 4 次前向饱和**——"教师好 42.8 FVD"全部来自 guidance。

### 4.6 时间粒度

物理时间粒度 Δt_c（每次因果提交覆盖的真实秒数）有**内部最优 0.533 s**
（FVD 700 / **499** / 852，两端各差 +40% / +71%）；两条不同路径到达同一 Δt_c
的 FVD 只差 **0.7%** ⇒ Δt_c 控质量、chunk 布局控**运动幅度**（同 Δt_c 两路径
运动量差 70%），两量分离。

### 4.7 Dynamics 的分解（本周核心产出）

- **e（跨速率弹性）**：同一批 clip、内容不变、观测位移放大 1.72×，模型输出
  只放大 1.01–1.02×（"忠实响应"被置信区间排除）；
- **b（跨场景斜率）**：动得多 10× 的场景，通用视频模型只多给 2.3×；
- **数据节奏干预只动水平**（+0.43, t=2.4）**不动响应度**（b 0.370→0.383）
  **且赔保真度**（FVD +75.2）⇒ 原结论按预注册判据撤回；
- **速度线性探针**（两域两模型、闭式岭回归、双对照）：观测速度在**每一层**
  线性可解码（R² 0.86–0.905）而输出不用它 ⇒ **杠杆在目标函数侧**；
- **机制**：低响应度 = **差分收缩**（快场景压得更狠），与方向不确定性无关。

### 4.8 负结果（同样进论文）

EqF 训练中程 +0.22 dB、6 万步归零；C11 时间方差匹配按 oracle 预测的代价关闭；
CHF 域依赖（机器人域有效、通用视频域三个可采列全负）；零 shot text-CFG 补救无效。

---

## 5 · 正在跑的任务与进度

| 作业 | 在做什么 | 进度 | ETA |
|---|---|---|---|
| code_drift_ctl 261038 | 从零训 r1_base 配方 ⇒ 从零阶梯 chunk2 格 + 预训练价值对照 | 4950/7500 | **1.4 h** + 打分 40 min |
| bridge_c3 258971 | Bridge 链收官（100k 预算，第三条走满）| 98200/100000 | **1.0 h** |
| bridge_final_coread 261312 | Bridge 终版并读 + 新展示片（已串在收官后）| 排队 | 收官后 ≈2 h |
| trackb_c4 261019 | 通用视频主线（150k 预算）| 46600/150000 | ≈125 h（分段接力）|
| e6_data_mix 259798 | 数据配比 G:A 五臂 → 能力前沿 | 臂 1/5 @4700/6000 | ≈41 h |
| c9v3_main_pair 259692 | guidance 内化（2 前向学生 vs 4 前向教师）| 1850/16000 | ≈28 h |
| s6_tdrop / s6_ctrl | text-CFG 训练侧修复配对 | 5200 / 2750（/7500）| 7 h / 13 h |
| rt1_degrade_mech 261309 | RT-1 退化机制：静态坍塌还是外观漂移 | 运行中 | ≈1 h |
| trackb_demo_refresh 261313 | 通用视频展示片刷新到当前 checkpoint | 排队 | ≈1 h |
| 排队 | E2/E3（CHF 机制 / onset）+ 面板、E7（动作进入时机）| 等 GPU 配额 | — |
| 就绪 | **E22a velmatch**（响应度干预第一臂）| 等 261038 落位 | 训+评 ≈6 h |

---

## 6 · 接下来的计划

1. **261038 落位**（约 1.4 h）→ 从零阶梯补齐 → **E22a 立即开跑**；
2. **Bridge 终版并读**（约 3 h）→ 三条链全部走满全预算、A3 三格全部钉在终版；
3. **E2 / E3**：CHF 是真机制还是历史污染正则、causal curriculum 是否存在；
4. **E6 / E7**：数据配比 → 能力前沿、动作进入时机；
5. **Track-B 150k 收官**后全部 demo 以终版重制，Physics-IQ 以"先越过冻结地板 21 分"为靶重测。

---

## 7 · Bridge 终版结果（收官后填入）

> 待 `bridge_c3`（100k）与 `bridge_final_coread`（261312）落地后更新本节与
> §4.1 表格；LangTable refresh 让数字变好（−0.18），RT-1 反而变差（+10.15），
> **方向不可预测，必须实测**。

---

## 8 · 三条阅读纪律（本项目用血换来的）

1. **每条运动结论必须同时报「水平 / 响应度 / 保真度」**——只动水平的干预不算改善 controllability；
2. **臂间比较核三样：配置、数据、以及 checkpoint 内嵌 config**——provenance 在产物里，
   不在 `configs/*.json` 里（CLI override 会让文件骗人，本周实际发生过）；
3. **每个 benchmark 先过两地板**（frozen + shuffled）——不可采的轴上，
   任何模型的高分（包括对手的）都不提供信息。
