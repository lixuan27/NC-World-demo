# NC-World 有效实验结果汇总

每一行都标注协议。**FVD 只在片数相同、片长相同、采样设置相同时可比**，跨协议并列无意义。
本文档只收录能追溯到结果文件的数字。

---

## 一、核心证据：因果设计强于双向设计

### 1.1 受控单变量对照（本项目最强的一组）

三臂**全部从随机初始化**，同一批数据，各训 **7500 步**，评测协议**逐字段相同**：
32 个片段 · cfg 1.0 · 每块 8 次去噪更新 · 在飞块数 1 · 缓存 4 块。
唯一变化的是注意力掩码与噪声调度。

| 臂 | FVD ↓ | LPIPS ↓ | PSNR ↑ | 响应度 b |
|---|---|---|---|---|
| 双向 + 整段共用一个噪声级 | 1566.3 | 0.1292 | 21.74 | 0.392 |
| 双向 + 逐块独立噪声 | **4454.4** | 0.5545 | 12.72 | **−0.006** |
| **块状因果 + 逐块独立噪声** | **690.8** | **0.0772** | **23.79** | **0.610** |

来源：`results/arm_panel/eval_r1_{bidirhom,bidirdf,base_recode}_R8.json`
与同名 `motion_r1_*_R8.json`。

**读法一（本项目最反直觉的结论）**：逐块独立噪声这项本意在于赋予流式能力的技术，
加在双向骨干上 **比不加还差 2.8 倍**（1566.3 → 4454.4），LPIPS 从 0.129 崩到 0.555，
PSNR 从 21.7 掉到 12.7。原因是它要教的那个不对称性在双向注意力下根本不存在：
干净块与噪声块互相可见，损失在教一件模型推理时永远遇不到的事。

**读法二**：把注意力换成块状因果后，**同一配方、同一评测协议**下 FVD 从 4454.4 降到 690.8，
**好 6.4 倍**；LPIPS 从 0.555 降到 0.077，好 7.2 倍。

**读法三（对双向最有利的比较）**：即便让双向骨干用它自己最有利的配置，也就是退回整段共用
一个噪声级，因果臂仍然好 **2.3 倍**（1566.3 → 690.8）。

**响应度 b 的含义**：场景实际运动量变为十倍时，模型输出运动量是否也大致变为十倍。
1 为忠实，0 为完全不跟随。双向加独立噪声那一臂 **b ≈ 0**，意味着它产出的运动量
与场景实际运动量**毫无关系**——这是四项像素指标都反映不出来的失效。

⚠️ **一处必须标注的边界**：第一行与第二行之间不是纯单变量。在本实现中，逐块独立噪声
与分块自回归推理协议是同一个设置：第一行是整段一次生成、无误差累积，后两行是分块自回归。
因此可以成立的读法有两条，且都不依赖那个混淆：
(a) 双向骨干无法当作分块流式模型使用（1566 → 4454）；
(b) 因果骨干即便对上双向骨干最有利的一次性生成协议，仍好 2.3 倍。
第二行与第三行之间是干净的单变量。

### 1.2 一处已撤回的说法

早期曾报因果对双向为 **8.9 倍**（4454 → 499）。该数字**混入了初始化差异**：
499 那一臂是从本项目更早的检查点热启动的，而 4454 是从零。
从零对从零的干净数字是 **6.4 倍**，本文档只用后者。

### 1.3 系统层面的对比

| 项 | 因果流式 | 双向 |
|---|---|---|
| 首帧延迟 | **188 ms** | 5.6 s |
| 吞吐（生产配置，真机域） | 48.6 fps ≈ 3.24× 实时 | 不适用 |
| 最长演示 rollout | 4097 帧（滚动缓存） | 受窗口长度限制 |

双向模型必须等整段去噪完成才能输出第一帧，因此首帧延迟相差约 30 倍。
这不是调优差距，是架构差距。

### 1.4 一组**不能**用作受控证据的对照（如实列出）

`results/oppoint/` 下有另一组因果 229.5 对双向 1087.1（81 帧 · 100 片），
四项指标全胜。但两臂的采样设置**逐项不同**：去噪步数 8 对 50、每块更新次数 2 对 10、
在飞块数 4 对 2、缓存 4 对 1，训练步数也差 500（7500 对 8000）。
**它不是单变量对照，不应作为因果优势的证据引用。**
本文档保留这一条只为说明它已被审查并排除。

---

## 二、本项目两项训练侧改动的价值

在长时程真机数据上，253 帧 · 24 片 · 采样设置逐项一致（cfg 2 · 每块 4 次更新 ·
在飞 3 · 缓存 3 · sink 1）。

| 配置 | FVD ↓ | 相对 |
|---|---|---|
| 两项都开 | **283.5** | 1.00× |
| 两项都关（忠实复现基线配方） | 629.9 | **2.22×** |

来源：`results/demo_final/eval_droid.json` 与 `results/sink_cfg/eval_faithful.json`。

两项改动分别是：训练时让模型自己生成紧接上下文的几个片段并把损失取在更后面的片段上；
以及把训练的噪声阶梯换成采样器推理时真正会走的那一族。

⚠️ 同一组对照曾在错误的采样工作点（在飞块数 8）下测出仅 **+13%**。
**在坏工作点上做的对照，其差值幅度不可信**，方向对了也可能差一个数量级。

---

## 三、外部基准并读

公开评测协议：官方验证划分与训练零重叠 · 8 帧动作条件续写 · 512 片段（RT-1 为 393）·
256px 打分 · 同一套度量实现。⚠️ 同名 FVD 在不同实现间可差 3 到 4 倍，故实现一致必须声明。

| 域 | 方法 | PSNR ↑ | SSIM ↑ | LPIPS ↓ | FID ↓ | FVD ↓ |
|---|---|---|---|---|---|---|
| **Bridge-V2** | 冻结画面地板 | 24.06 | **0.8821** | 0.0772 | 17.71 | 238.70 |
| | IRASim | — | — | — | — | 91.60 |
| | DriftWorld | — | — | — | — | 101.20 |
| | 本项目（生产设置） | 26.19 | 0.8701 | 0.0613 | 14.26 | 48.21 |
| | **本项目（感知设置）** | **26.81** | 0.8754 | **0.0572** | **14.20** | **45.08** |
| **Language-Table** | 冻结画面地板 | 23.08 | 0.8886 | 0.0857 | 14.66 | 167.31 |
| | IRASim | — | — | — | — | 60.05 |
| | DriftWorld | — | — | — | — | 39.09 |
| | 本项目（生产设置） | 29.34 | 0.9025 | 0.0343 | 10.85 | 38.97 |
| | **本项目（感知设置）** | **29.62** | **0.9028** | **0.0333** | **10.23** | **37.97** |
| **RT-1** | 冻结画面地板 | 23.88 | 0.8732 | 0.0923 | 33.83 | 459.53 |
| | DriftWorld | — | — | — | — | **68.72** |
| | 本项目（生产设置） | 26.80 | 0.8785 | 0.0606 | **17.99** | 82.44 |
| | 本项目（感知设置） | **27.42** | **0.8827** | **0.0563** | 18.04 | 79.47 |

⚠️ **Bridge-V2 上冻结画面地板的 SSIM（0.8821）高于本项目（0.8754）。**
一个全程输出静止画面的基线在结构相似度上赢过我们，说明 SSIM 在该域该时长下
**不是可用来支撑动态性主张的指标**。保留此行而非删除。

⚠️ **RT-1 是唯一负格**，且其 FVD 随训练**单调上升**（10k 69.32 → 20k 76.32 → 30k 79.47）
而 LPIPS 持平，说明不是预算不足而是训练后期的分布层面退化。
我们报训练终点而非事后挑最优检查点。Bridge 也同向但速率差 2.7 倍
（每万步 +2.7% 对 +7.3%），**同向不等于同一现象**。

---

## 四、长时程消融（253 帧 · 24 片 · 协议逐项一致）

### 4.1 长上下文课程

| 阶段 | 训练窗口 | FVD ↓ | PSNR ↑ | 相对 |
|---|---|---|---|---|
| stage-1 | 45 帧 | 513.9 | 19.64 | — |
| stage-2a | 125 帧 | 350.5 | 19.54 | −31.8% |
| stage-2b | 253 帧 | **296.0** | **19.68** | **−42.4%** |

三格采样设置完全一致（cfg 2 · steps 8 · ar 2 · 在飞 3 · 缓存 3）。
**FVD 单调改善 42% 而 PSNR 持平**：课程买到的是分布层面的长时程一致性，不是逐帧锐度。

⚠️ 早期曾把末格写成 274.8，那是另一个采样设置下的数字，与前两格不匹配，已更正为 296.0。

### 4.2 采样器：两个旋钮乘积相同、方向相反

| 在飞块数 | FVD ↓ | PSNR ↑ |
|---|---|---|
| 3 | **296.0** | **19.68** |
| 8 | 724.5 | 16.35 |

单变量：同权重、同 cfg、同去噪步数、同缓存，只改在飞块数。**加宽流水线代价 2.45 倍。**

| 每块去噪更新次数（LIBERO · 49 帧 · 24 片） | FVD ↓ | PSNR ↑ | LPIPS ↓ |
|---|---|---|---|
| 2 | 64.33 | 31.50 | 0.0228 |
| 4 | 59.34 | **34.12** | **0.0188** |
| 8 | **56.52** | 33.99 | 0.0190 |

两者乘积就是通常所说的采样步数，但方向相反。机制：加宽流水线等于让一个块在提交时
所依赖的邻居更噪，改变的是历史的噪声水平而非算力总量。

### 4.3 引导强度（存在内部最优）

| cfg | FVD ↓ | PSNR ↑ | LPIPS ↓ | SSIM ↑ |
|---|---|---|---|---|
| 1.00 | 317.2 | **19.78** | 0.1328 | **0.7863** |
| 1.25 | 289.8 | 19.76 | **0.1325** | 0.7849 |
| **1.50** | **274.8** | 19.71 | 0.1337 | 0.7827 |
| 2.00 | 283.5 | 19.61 | 0.1363 | 0.7777 |
| 3.00 | 337.8 | 19.37 | 0.1458 | 0.7623 |

1.5 是真正的内部极小值。此前全项目默认取 2，且从未在机器人域扫过。

### 4.4 永久锚点帧数（负结果）

| sink 帧数 | FVD ↓ |
|---|---|
| 1 | **283.5** |
| 2 | 284.7 |
| 4 | 323.5 |

**更宽的锚点买不到东西** ⇒ 长时程漂移不是"模型失去初始条件"那类失效，该假设关闭。

### 4.5 提交粒度（存在内部最优，且等于延迟下限）

| 一次因果提交覆盖的物理时间 | FVD ↓ | 响应度 b |
|---|---|---|
| 0.27 s | 699.6 | 0.456 |
| 0.53 s | **690.8** | 0.610 |
| 1.07 s | 852.1 | **0.892** |

粗粒度换来响应度（0.46 → 0.89）但代价是 FVD +23%。而 0.53 s 恰好等于独立测出的
动作到画面变化的延迟下限。**历史变得不可撤销的那个时间量子，同时决定了可控性上限与质量最优点。**
⚠️ 三格的缓存块数不同（12 / 4 / 2），因为粒度本身改变缓存需求，故**不与 1.1 节并列**。

---

## 五、评测天花板与地板

### 5.1 自编码器天花板（证明编解码器不是瓶颈）

把真值视频经同一个自编码器编解码往返后当作生成结果打分：

| 帧数 | PSNR ↑ | SSIM ↑ | LPIPS ↓ |
|---|---|---|---|
| 21 | 36.21 | 0.9727 | 0.0077 |
| 253 | 35.38 | 0.9641 | 0.0095 |

**只掉 0.83 dB** ⇒ 自编码器的损失是空间性的，**不随时长累积** ⇒ 长时程伪影不是编解码造成的。
本项目最好的模型是 19.71 dB / 0.1337，**只用掉可用余量的 56%，LPIPS 差 14 倍**。

### 5.2 误差累积的形状

253 帧连续生成的逐段 PSNR：首段 **28.27** → 第 50 帧附近 **19.5** → 末段 **17.86**，
整体衰减 6.67 dB。**前 50 帧掉 8.8 dB，之后 200 帧只再掉 1.6 dB。**

⇒ **损失是前置的，不是累积的**：主要发生在模型第一次开始依赖自己输出的那一刻。
该攻的是最初两秒。

### 5.3 双地板可采纳性检验（负结果）

对某常用视频基准做 do-nothing（冻结画面）与 random-motion（打乱帧序）双地板检验：
**九个维度无一可用**，其中四个被冻结画面拿到满分（1.0000 / 1.0002 / 0.9983 / 1.0000），
另外五个被打乱帧序追平。

⇒ 硬规则：**一个指标只有在什么都不做和乱动都拿不到好分数时，才配支撑运动正确性的主张。**

---

## 六、动作依赖度（跨域，非跨域可比）

同一探针：真动作、打乱动作、空动作下的流匹配损失，报 `(打乱 − 真) / 真`。

| 域 | 真动作损失 | 差值 | 比值 |
|---|---|---|---|
| LIBERO（固定机位） | 0.0707 | +0.0779 | **110%** |
| Language-Table（固定机位） | 0.0732 | +0.0697 | 95% |
| Bridge-V2 | 0.2379 | +0.0900 | 38% |
| RT-1 | 0.1827 | +0.0337 | 18% |
| 真机 DROID（手持机位） | 0.3260 | +0.0235 | 7.2% |

⚠️ **跨行不可直接比较**。固定机位域里机械臂几乎是画面中唯一变化源，天花板本就高；
手持机位域的背景与光照独立于动作变化，天花板本就低。这是**域内诊断**，不是排名。

---

## 七、各域绝对成绩（本项目协议）

| 域 | 帧 | 片 | PSNR ↑ | SSIM ↑ | LPIPS ↓ | FVD ↓ |
|---|---|---|---|---|---|---|
| LIBERO | 49 | 24 | 33.99 | 0.9697 | 0.0190 | 56.5 |
| Language-Table | 41 | 24 | 28.26 | 0.9047 | 0.0425 | 168.0 |
| Bridge-V2 | 77 | 23 | 22.58 | 0.8547 | 0.0748 | 251.7 |
| 真机 DROID | 253 | 24 | 19.71 | 0.7827 | 0.1337 | 274.8 |

⚠️ Bridge-V2 这一行是 **77 帧**展示协议，与第三节的 **9 帧**外部并读协议**不同源**，
两者的数字不可互相对应。77 帧是该数据集的硬上限（最长留出 episode 只有 79 帧）。

---

## 八、系统能力

| 项 | 数值 |
|---|---|
| 流式吞吐（生产配置，真机域） | 48.6 fps ≈ 3.24× 实时 |
| 首帧延迟 | 188 ms（双向 5.6 s） |
| 在线重规划省算 | 36–74%，输出与全量重算逐位相同（16/16） |
| 动作到画面结构的延迟下限 | 533 ms = 一次因果提交的物理时间粒度 |
| 最长演示 rollout | 4097 帧（滚动缓存，无重置） |

---

## 九、正在进行

| 臂 | 架构 | 当前步 | 目标 | 状态 |
|---|---|---|---|---|
| 1B | 28 层 · 宽 1536 · 12 头 | 31,000 | 40,000（stage-1a） | 续接链排队 |
| 3B | 32 层 · 宽 2560 · 20 头 · 2,908,267,212 参数 | 10,000+ | 40,000 | 运行中 |

两条链**除模型尺寸预设外逐字段相同**，全部从随机初始化，语料为全量 57,772 集。
40k 步处有**预注册的证据门控**（判据已 committed）：CONTINUE 3B 当且仅当
FVD 更低且 LPIPS 不更差，否则就地截断。
"匹配步数下 3B 不明显优于 1B"本身是可发表结果，不是调阈值的理由。
