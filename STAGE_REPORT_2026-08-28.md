# NC-World 阶段报告 · 2026-08-28

NC-World 要回答一个很具体的问题：一个**原生因果的视频世界模型**（native causal video world model），能不能在学术实验室可负担的算力下**从随机初始化训练出来**，并且好到可以拿去做流式渲染和规划。每个词都是约束。"世界模型"意味着它不只生成好看的视频，而要在给定历史观测与控制信号（机器人动作）之后，预测下一步世界会变成什么样；"因果"意味着每一帧只能看它之前的帧，不能像常规视频扩散模型那样双向地看整段；"原生"意味着不走"拿一个预训练好的双向视频生成模型再后训练成自回归"这条主流路线，而是一开始就按因果方式训练——因为双向预训练与因果流式推理之间存在结构性不匹配，后训练只能缓解不能消除。"从零"是本项目的立场：不从任何已发布权重继续训练。2026-08-27 起，项目范围收窄为**纯动作条件的机器人数据**，文本条件的通用视频线已终止（原因见后）。

模型本体是一个 **block-causal Video DiT**，跑在一个冻结的 Wan2.2 视频 VAE 的潜空间里。这个 VAE 在时间上压 4 倍、空间上压 16 倍，输出 48 通道；240×320 的一帧对应 15×20 的潜网格，每个 latent frame 300 个 token。DiT 的 patch size 是 1，一个 latent voxel 一个 token。注意力是 block-causal 的：视频切成时间上有序的 chunk（每 chunk 两个 latent frame，即 8 个 RGB 帧），chunk 内部双向自由交互，chunk 之间严格单向——第 k 个 chunk 只能看 1..k。这样既保住 chunk 内的时空联合建模，又满足自回归流式生成所需的因果依赖。位置编码用三轴分解的 RoPE，并支持把缓存里的 key 按整数帧数重新旋转——这是滚动 KV cache 能在不重算的前提下"重新编号"位置的关键。

训练目标是 **rectified flow / flow matching**，配合 **Diffusion Forcing**：同一段视频里不同 chunk 被独立地加不同强度的噪声，而不是整段共享一个噪声水平。这一步是整个方法的枢纽——它让模型天然学会"在已经比较干净的历史条件下、去噪一个还很脏的未来"，而这正是流式推理时每一刻的真实处境。噪声水平的采样遵循 chunk 级非降序约束（越早的 chunk 噪声越低），在所有合法整数序列上均匀采样，时间步再按 SD3 方式做 shift。训练时随机在两种 in-context 模式间切换：一半概率只有第 0 帧干净（image-to-video），一半概率整个第一个 chunk 干净（video-to-video），于是一个 checkpoint 同时服务两种推理场景。

在这套标准配方之外，本项目加了两件自己的东西，它们是当前唯一被受控实验证明有效的方法级改动。第一件是 **schedule-aligned noise sampling**：训练时的噪声调度以一定权重向"推理时实际会走的那条阶梯"靠拢——因为推理时的噪声轨迹是被异步流水线决定的一个很窄的子集，而均匀采样把大量预算花在了推理永远不会经过的组合上。第二件是 **committed-history forcing**：训练时以一定概率把"已经提交进 KV cache 的历史"当成干净上下文喂回去，模拟推理时历史确实已定稿这一事实，缓解训练与推理之间的 exposure bias。两项一起，在受控对照下值 **2.2 倍 FVD**（关掉后 283.5 → 629.9，同一采样设置）。

动作条件走双路：动作向量经一个 MLP 得到 embedding 加进 diffusion timestep 那一路，再经另一个 MLP 得到 AdaLN 调制量直接加到每个 Transformer block 的调制参数上，后者零初始化，所以训练开始时动作影响为零、随训练逐渐生长。DROID 域的动作是 7 维（6 自由度末端位姿 + 夹爪），按语料级 1%/99% 分位数归一化到 [-1,1]，四个连续 RGB 帧的动作拼成一个 28 维的 latent-frame 动作 token。这里有一个踩过并纠正过的坑：有效的量是**绝对位姿**而非位移——把训练充分的模型改喂位移，动作依赖度从 +24.1% 掉到 −2.9%（打乱动作反而比真动作更好），FVD 从 89.9 崩到 680。

推理是流式的。任意时刻序列分成两部分：已完成去噪、K/V 已提交进 cache 的**历史**，和若干个仍在不同噪声水平上、正被并行去噪的**在飞 chunk**。一个 chunk 走到噪声为零就被提交，之后永不重算。cache 满了就 FIFO 淘汰最老的非 sink 帧，剩下的 key 按淘汰帧数整体反向旋转，使位置重新从 0 编号，从而始终待在训练见过的 RoPE 范围内；第一个 chunk 作为 sink anchor 永久保留。这样活动窗口的计算量固定，视频长度可以无限延伸。

这里有一个最重要也最反直觉的工程发现：**同时在飞的 chunk 数（pipeline width）与每个 chunk 得到的去噪更新次数（ar_step）是两件事，效应符号相反**。两者相乘等于"有效步数"，所以很容易被合称为"深度"——但在单变量对照下（同权重、同 cfg、同 cache），把在飞 chunk 数从 3 提到 8 使 FVD 从 296.0 崩到 724.5（2.45 倍），而在固定宽度下增加更新次数反而更好（LIBERO 上 64.3 → 59.3 → 56.5）。机制上讲得通：在飞的 chunk 越多，一个 chunk 提交时所注意到的邻居里"仍然很噪"的就越多，它是在对一批噪声邻居做条件预测。

训练数据是 LeRobot 格式的 DROID 发布版。**这里是本阶段最重要的发现，它改写了"结果差是因为算力不够"这个默认判断。** 数据集自身的元数据声明 **57,774 个 episode / 14,748,517 帧 / 273 小时**，而盘上此前只有 **14,000 个 / 66 小时 = 24%**——当初的下载清单截断在 chunk-013，之后无人回头补。把这个缺口和训练预算对上就很清楚：原四段课程共消耗 36.9 B token，而已下子集的唯一 token 只有 0.323 B，即**等效 114 轮**；决定长时程质量的 stage-2b 更极端，448,000 个窗口全部来自 5,040 个够长的 episode，**每集被抽 89 次**。在 114 轮这个比值下，多加的算力主要在记忆而不是学习——一个 0.55B 模型吃 36.9B token 本就已是 Chinchilla 最优的 3.4 倍。补下缺的 34 GB 后，语料变成 **57,772 集可用（声明总量的 99.99%，扩容前的 4.13 倍）**，训练成本为零。

即便补齐，仍有一个加钱解决不了的限制：64 latent-frame 的窗口需要 253 个 RGB 帧，全量语料里也只有 **37.1%** 够长。而且这个筛选**不是随机的**——按任务动词聚合，`use` 85.7%、`pour` 75.4%、`fold` 65.7% 的 episode 够长，而 `press` 只有 10.1%、`push` 16.0%、`close` 20.7%。也就是说，决定长时程质量的那段课程，把快速、离散接触的动作基元看成真实频率的四分之一到一半。修法在配方侧（stage-2b 内按批次交替 125/253 帧窗口，125 帧可用率 85%），架构上无障碍。⚠️ 但这条偏置**是否真的转化成质量差尚未验证**，判据应是按动词分组的 FVD/PSNR。

其余训练设置：优化器 Muon，stage-1 学习率 1e-4、stage-2 降到 2e-5 并带 warmup，梯度裁剪 1.0，EMA 0.9999，bf16 混合精度，50 个离散时间步 bin，logit-normal 时间步采样。全局 batch 在 stage-1 是 64、stage-2 是 16，这个数字在代码里是 fail-closed 校验的：world × per-rank × grad accumulation 必须严格等于配置里声明的值，否则直接中止——因为"静默地按另一个 batch 训练"正是会让整组对比作废的那类错误。per-rank 与 accum **不写死在配置里**，而由启动器按 SLURM 实际给的卡数推导，所以同一份配置在 4 / 8 / 16 卡下都配平。

整个流程可以这样看：

```
                              ┌─────────────────────────────────────────┐
   原始视频 + 动作             │  Wan2.2 VAE (frozen)                    │
   ─────────────────────────▶ │  时间 4× · 空间 16× · 48 通道            │
   240×320 RGB               │  逐通道 (z − mean) / std 归一化           │
   动作 7-D（绝对位姿+夹爪）   └──────────────────┬──────────────────────┘
                                                 │  latent 15×20×48
                                                 ▼
   ┌──────────────────────────────────────────────────────────────────┐
   │  训练：Block-Causal DiT + Rectified Flow + Diffusion Forcing      │
   │                                                                  │
   │   chunk 切分（2 latent frame = 8 RGB frame）                      │
   │        ↓                                                         │
   │   每个 chunk 独立采样噪声水平，且沿时间非降序                        │
   │   ├── 标准配方：在所有合法非降序序列上均匀采样                        │
   │   └── 本项目 C1：向推理实际走的阶梯对齐（schedule-aligned）          │
   │        ↓                                                         │
   │   动作条件注入                                                    │
   │   ├── 动作 → MLP → 加进 timestep 流                               │
   │   └── 动作 → MLP → 零初始化 AdaLN 残差（每 block 低秩 rank 256）    │
   │        ↓                                                         │
   │   本项目 C2：以概率把已提交历史当干净上下文喂回（committed-history） │
   │        ↓                                                         │
   │   flow matching 损失（只在预测 chunk 上，历史 chunk 只作条件）       │
   └──────────────────────────────┬───────────────────────────────────┘
                                  │
                     四段窗口课程（每段接上一段权重，全部从零起）
      21 帧 ──→ 45 帧 ──────────→ 125 帧 ──────────→ 253 帧
      lat 6     lat 12            lat 32             lat 64
      40k 步    42k 步            28k 步             28k 步     ← 2026-08-28 新预算
      batch 64  batch 64          batch 16           batch 16
      lr 1e-4   lr 1e-4           lr 2e-5            lr 2e-5
                                  │
                                  ▼
   ┌──────────────────────────────────────────────────────────────────┐
   │  流式推理：Rolling KV Cache + 异步去噪阶梯                          │
   │                                                                  │
   │   [ sink anchor ][ 已提交历史 (FIFO) ][ 在飞 chunk × N ]           │
   │         永久          淘汰后整体反向旋转      各处不同噪声水平        │
   │                                                                  │
   │   一个 chunk 噪声归零 → 提交 K/V → 永不重算 → 解码出 RGB            │
   │                                                                  │
   │   ⚠ N（pipeline width）与 ar_step（更新次数）效应符号相反            │
   │     实测最优：N=3 · ar_step=4 · guidance=1.5                       │
   └──────────────────────────────┬───────────────────────────────────┘
                                  ▼
                    评测：绝对指标 + do-nothing 地板 + 自编码器上限
             PSNR / SSIM / LPIPS / FID / FVD · 动作依赖探针 · 响应度 b
```

现在说已经产出的结果。**最硬的一组是外部并读**：在公开协议下（IRASim 官方验证 split，与训练零重叠，8 帧动作条件续写，512 集，同一套 I3D 权重与预处理约定——同名的 FVD 在不同实现间可差 3–4 倍，所以这一条很重要）。Bridge-V2 我们是 **FVD 45.08**，对读 IRASim 的 91.6 与 DriftWorld 的 101.2，冻结帧地板 238.7；Language-Table 我们是 **FVD 37.97**，对读 IRASim 60.05、DriftWorld 39.09，冻结地板 167.3。RT-1 是唯一负格：训练终点 30k 步是 79.47，而 DriftWorld 是 68.72；这条链的 FVD 随训练**单调上升**（10k 69.32 → 20k 76.32 → 30k 79.47）而 LPIPS 持平，所以不是预算不足而是训练后期的分布层面退化，10k 处与对手相差 0.9%。我们仍报训练终点而不事后挑最优点。⚠️ 另一处必须讲清的：**Bridge-V2 上冻结帧地板的 SSIM（0.8821）高于我们（0.8754）**——一个全程输出静止帧的基线在结构相似度上赢过我们，这说明 SSIM 在该域该时长不是可采纳的动态性指标，我们保留这一行而非删掉它。

**第二组是因果性本身的证据**，这是项目的核心论点。全部从随机初始化、同一批 clip、各训 7500 步、同一评测协议：双向骨干加 diffusion forcing 是 FVD 4454.4、响应度 b = −0.006（输出运动量与场景实际运动量**零相关**）；同一双向骨干换成同质噪声是 1566.3、b = +0.392；换成 block-causal 之后（同配方、同分块自回归协议）是 **690.8、b = 0.610**。也就是说，**在双向骨干上直接用 diffusion forcing 会让模型比不用它还差 2.8 倍，而换成因果注意力后同配方同协议好 6.4 倍**。因果性不是与 diffusion forcing 并列的一项选择，它是 DF 可用的**前提条件**。⚠️ 早期曾报过 8.9 倍，那个数混入了初始化差异（对照臂是 warm-start），已更正为从零对从零的 6.4 倍。

**第三组是 DROID 长时程的完整消融**，全部在 253 帧、24 集、协议逐项一致的条件下：训练课程沿 stage-1（45 帧窗口）→ stage-2a（125 帧）→ stage-2b（253 帧）给出 FVD **513.9 → 350.5 → 296.0**，单调改善 42%，而 PSNR 基本持平——课程买到的是分布层面的长时程一致性，不是逐帧锐度。采样器方面，同一权重把在飞 chunk 数从 3 提到 8 是 **296.0 → 724.5**（2.45 倍，单变量）；引导强度扫了五档，**1.5 是真正的内部极小值**（1.0→317.2、1.25→289.8、**1.5→274.8**、2→283.5、3→337.8）；永久 sink anchor 帧数扫 1/2/4 给出 283.5/284.7/323.5，**更宽的锚点买不到东西**，所以长时程漂移不是"模型失去初始条件"那类失效。方法消融方面，关掉我们自己的两项训练侧改动后是 **283.5 → 629.9**（2.22 倍，同为 cfg 2）。

**第四组是评测的天花板与地板**，这是保证前面所有数字可解释的基础设施。DROID 上把真值视频经同一个 VAE 编码再解码、然后当作生成结果打分，21 帧处是 36.21 dB / LPIPS 0.0077，253 帧处是 35.38 dB / 0.0095——**只掉 0.83 dB**，说明自编码器的损失是空间性的、不随时长累积，因此长时程伪影**不是编解码造成的**。我们目前最好的 DROID 模型是 19.71 dB / 0.1337，只用掉了 **56%** 的可用余量、LPIPS 差 14 倍。漂移曲线的形状也值得注意：PSNR 在前 50 帧从 28.3 掉到 19.5，之后 200 帧只再掉 1.6 dB——**损失是前置的，不是累积的**，所以攻长时程质量该攻头两秒而非尾巴。地板方面，我们对公开 benchmark 做过 do-nothing 与 random-motion 双地板检验，结论是 VBench 的全部九个维度**没有一个可采纳**——四个被"冻结帧"最大化（1.0000/1.0002/0.9983/1.0000），五个被"随机打乱帧序"打平。这条纪律现在是硬规则：**一个指标只有在"什么都不做"和"乱动"都拿不到好分数时，才配承载运动正确性的主张**。

**第五组是系统能力**：真机域生产配置下流式吞吐 48.6 fps，即 3.24 倍实时；首帧延迟 188 ms，而双向模型是 5.6 秒；在线重规划省算 36–74% 且输出与全量重算逐位相同（16/16）；动作到画面结构的延迟地板是 533 ms，恰好等于一次因果提交的物理时间粒度，实验证实它同时也是质量最优点。

**第六组是已终止的通用视频线留下的东西**，它比那条线本身有用。该域响应度 b 只有 0.29–0.52，而同骨干在动作条件域是 0.46–0.89；引导完全无效（cfg 2/4/7 给出 b = 0.288/0.269/0.366，无趋势），两条损失侧路线都按预注册判据关闭。**模型缺的是"该动多快"这个信号本身**——动作向量明确指定机械臂移动多少，而"一个人骑自行车"不说骑多快。这条线临终留下一个方法学结果：我们曾报"把每 chunk 去噪更新次数从 4 提到 8 能把 b 从 0.288 抬到 0.466"，**抽帧逐张看之后该结论作废**——ar 4 和 8 的 rollout 都在第 40 帧崩解成抽象色块，而**画面解体会产生极大的光流**，所以光流比值型指标把崩解打成了"运动忠实"。只有 ar_step=2 撑满 81 帧且 b 最高（0.517）。⇒ **任何以运动类指标为主判据的选择，必须先过抽帧目视门控，再用指标排序。**

其他域的当前数字：LIBERO 在 49 帧 24 集协议下是 PSNR 33.99 / LPIPS 0.0190 / FVD 56.5；Language-Table 在 41 帧协议下是 28.26 / 0.0425 / 168.0；Bridge-V2 的展示片长度是 **77 帧**——那是**数据集的硬上限**（最长的 held-out episode 只有 79 帧），实测 22.58 / 0.0748 / 251.7。⚠️ Bridge 的展示片长度（77 帧）与其对外 benchmark 行（9 帧续写协议）**不同源**，不要把观感对应到那个数字上。

现在说正在进行的任务。**主线是 1B 与 3B 两档规模的从零训练**，0.5B 已于 2026-08-27 停掉以集中算力。两条阶梯**除 `model.preset` 一个字段外逐字段相同**——同样的四段窗口课程、步数、全局 batch、学习率与噪声调度、动作接口、语料、窗口采样，全部从随机初始化。1B 是 28 层 / 宽 1536 / 12 头；3B 是 32 层 / 宽 2560 / 20 头，实测 2,908,267,212 参数。规模是唯一变量，这才让"剩余伪影是容量问题"这一假设可证伪。

为在 7 天内跑完 3B，2026-08-28 做了两处配方改动。**一是步数预算从 236,000 砍到 138,000**（40k/42k/28k/28k），等效轮次从 33.3 降到 **24.6**——注意扩容前是 114 轮的过训区间，24.6 轮仍在健康范围。`s1a` 保满 40,000 步，因为已预注册的 40k 证据门控设在那里，砍它会让门控失去参照。**二是 stage-2 的 global batch 从 8 提到 16**，这解开了一个人为的并行度天花板：`world × per-rank × accum = global_batch` 且 per-rank 在 lf=64 时已是 1（单卡 74 GB，再大 OOM），所以 batch 8 把卡数锁死在 8。提到 16 后 world 上限翻倍，**单卡显存完全不变**。⚠️ 这是真正的配方改动，两臂同改以保 1B/3B 可比，但**新的 stage-2 结果与历史的 `b_s2b`（FVD 296.0 / 274.8）不再直接可比**。

当前进度：**1B 在 step 16,000 / 40,000**（s1a，40%），**3B 在 step 6,000 / 40,000**（15%）。两条链此刻都在排队等 GPU——上一批作业撞 12 小时墙钟结束后释放了卡。墙钟已按需求提到 **4 天**（分区上限 4-04:00:00），这把全程链接数从 49 个降到 **9 个**，3B 的 s1a（69 小时）现在一个链接就能跑完，不再中途回到队尾。⚠️ 代价是 4 天的作业更难被 backfill 挤入，已挂一个可证伪的监控：若 2 小时内有空卡却仍未起跑，即为 4 天墙钟挡住 backfill 的直接证据，届时退回较短墙钟。

按实测速率（1B 稳态 0.39 it/s、3B 0.16 it/s，均 4 卡）估算：4 卡下 1B 全链约 293 小时、3B 约 685 小时。要压进 7 天需要 16 卡，而 16 卡是多机——多机 smoke（2 节点 × 8 卡）已提交并在排队，**它通过之前不会把 4 天的链扔上多机**。这个工作区为多机 NCCL 卡死烧过两次 30 分钟，所以规则 18–22 全套配置已就位：完整 NCCL 环境变量、`MASTER_ADDR` 非空校验、`MASTER_PORT` 由作业号 derive（不用 `$RANDOM`，不同 srun task 会错位）、`srun --kill-on-bad-exit=1`，而 `init_process_group` 本就显式传了 `device_id`——那正是两次卡死的根因。smoke 还加了一条断言而非只看退出码：必须在日志里见到真实的 `[train] step=` 才算通过，因为卡死被 kill 也会非零退出（原因却是错的），而卡死若熬过墙钟反倒像通过。

预期结果是这样的。首先，**规模应当在长时程一致性上体现得比在逐帧锐度上更明显**——因为课程（本质上也是"看更长的上下文"）改善的是 FVD 而不是 PSNR，容量应当作用在同一维度。所以预期 FVD 随规模显著下降，PSNR 改善温和。其次，**动作依赖度应当随规模与数据覆盖上升**：目前 DROID 模型的 gap/true 只有 7.2%，而固定机位的 LIBERO/LangTable 能到 95–110%，DROID 因手持机位、背景与光照独立于动作变化，天花板本就低（训练充分的模型实测是 24.1%），所以预期 1B/3B 能把 7.2% 推向 20% 上下。第三，**自编码器余量应当被显著吃掉**：现在只用了 56%，若规模研究把 PSNR 从 19.7 推到 24 以上、LPIPS 从 0.134 压到 0.06 以下，就是把余量用到 68% 以上。第四，预期**在 40k 步的门控处就能看出两档的排序**；若 3B 在匹配步数下不明显优于 1B，那本身是一个值得报告的负结果——它会说明在这个数据规模下容量不是瓶颈。

最后是几条本阶段用真金白银换来的方法学纪律。**一个指标只有在 do-nothing 和 random-motion 双地板下都拿不到好分数时才可用**——因此判定 VBench 九个维度全部不可采纳。**FVD 只在 clip 数相同、片长相同的条件下可比**，跨协议的绝对值并列无意义。**在坏工作点上做的对照，其差值幅度不可信**——同一方法消融在错误采样设置下测出 13%、在正确设置下测出 122%。**运动类指标会把画面崩解打成高分**，因为解体产生极大光流，所以任何以运动指标为主判据的选择必须先过目视门控。**用字符串或模式去指认一个具体对象会匹配到自己或陈旧对象**——`pkill -f` 杀掉执行它的 shell、`pgrep -f` 匹配到监控自身导致整条流水线永久静默停死、`ls -t <glob>` 取到已取消作业的日志，正解一律是 PID 文件、显式 PID、按作业号。**同一事实不要存两个副本**——每卡批量写死在配置里、维护器写死目标步数，改一处就埋雷，正解是从唯一事实源派生。**诊断"训练不充分"前先算 [预算 token ÷ 唯一 token] = 等效轮数**，114 轮说明该加数据而不是加算力。
