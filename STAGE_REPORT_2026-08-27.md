# NC-World 阶段报告 · 2026-08-27

NC-World 想回答的是一个很具体的问题：一个**原生因果的视频世界模型**（native causal video world model），能不能在学术实验室可负担的算力下**从随机初始化训练出来**，并且好到可以拿去做流式渲染和规划。这里的每一个词都是约束。"世界模型"意味着它不只是生成好看的视频，而要在给定历史观测和控制信号（机器人动作、相机位姿、或者文本）之后，预测下一步世界会变成什么样；"因果"意味着每一帧只能看它之前的帧，不能像常规视频扩散模型那样双向地看整段；"原生"意味着不走"拿一个预训练好的双向视频生成模型再后训练成自回归"这条主流路线，而是一开始就按因果的方式训练——因为双向预训练与因果流式推理之间存在结构性的不匹配，后训练只能缓解不能消除。"从零"则是这个项目的立场：不从任何已发布权重继续训练，全部从随机初始化开始。

模型本体是一个 **block-causal Video DiT**，跑在一个冻结的 Wan2.2 视频 VAE 的潜空间里。这个 VAE 把视频在时间上压 4 倍、空间上压 16 倍，输出 48 个通道；240×320 的一帧对应 15×20 的潜网格，也就是每个 latent frame 300 个 token。DiT 的 patch size 是 1，即一个 latent voxel 一个 token，不再做额外的空间分块。注意力是 block-causal 的：视频被切成时间上有序的 chunk（每个 chunk 两个 latent frame，也就是 8 个 RGB 帧），chunk 内部双向自由交互，chunk 之间严格单向——第 k 个 chunk 只能看 1..k。这样既保住了 chunk 内的时空联合建模能力，又满足了自回归流式生成所需的因果依赖。位置编码用三轴分解的 RoPE，时间、高、宽各分走一部分 head 维度，并且实现了一个额外能力：缓存里的 key 可以按整数帧数重新旋转，这是滚动 KV cache 能在不重算的前提下"重新编号"位置的关键。

训练目标是 **rectified flow / flow matching**，配合 **Diffusion Forcing**：同一段视频里不同的 chunk 被独立地加不同强度的噪声，而不是整段共享一个噪声水平。这一步是整个方法的枢纽——它让模型天然学会"在已经比较干净的历史条件下、去噪一个还很脏的未来"，而这正是流式推理时每一刻的真实处境。噪声水平的采样不是随意的：我们复现了 chunk 级的非降序约束（越早的 chunk 噪声越低）以及一个在满足该约束的所有整数序列上均匀采样的调度器，时间步再按 SD3 的方式做 shift，shift 因子由每个 chunk 的 token 数决定。训练时还随机在两种 in-context 模式间切换：一半概率只有第 0 帧是干净的（image-to-video），一半概率整个第一个 chunk 都是干净的（video-to-video），这样一个 checkpoint 同时服务两种推理场景。干净上下文会被加一个很小的随机噪声（上限 0.05），动作条件以 10% 概率被替换成一个学习得到的 null token 以支持 classifier-free guidance。

在这套标准配方之外，本项目加了两件自己的东西，它们是当前唯一被受控实验证明有效的方法级改动。第一件是 **schedule-aligned noise sampling**：训练时的噪声调度不完全按均匀采样，而是以一定权重向"推理时实际会走的那条阶梯"靠拢——因为推理时的噪声轨迹是被异步流水线决定的一个很窄的子集，而均匀采样把大量预算花在了推理永远不会经过的组合上。第二件是 **committed-history forcing**：训练时以一定概率把"已经提交进 KV cache 的历史"当成干净上下文喂回去，模拟推理时历史确实是已定稿的这一事实，缓解训练与推理之间的 exposure bias。这两项一起，在受控对照下值 **2.3 倍 FVD**（关掉后 274.8 → 629.9）。

动作条件走双路：动作向量经一个 MLP 得到一个 embedding 加进 diffusion timestep 那一路，再经另一个 MLP 得到一个 AdaLN 调制量直接加到每个 Transformer block 的调制参数上，后者零初始化，所以训练开始时动作影响为零、随训练逐渐生长。每个 block 的调制网络是共享主干加一个低秩残差（AdaLN-LoRA，秩 256），也是零初始化。DROID 域的动作是 7 维（6 自由度末端位姿 + 夹爪），按语料级 1%/99% 分位数归一化到 [-1,1]，四个连续 RGB 帧的动作拼成一个 28 维的 latent-frame 动作 token。这里有一个我们踩过并纠正过的坑：论文文字写的是"位移"，但实际有效的量是**绝对位姿**——把一个训练充分的模型改喂位移，它的动作依赖度会从 +24.1% 掉到 −2.9%（打乱动作反而比真动作更好），FVD 从 89.9 崩到 680。所以现在全线用绝对位姿。

推理是流式的。任意时刻，序列分成两部分：已经完成去噪、K/V 已提交进 cache 的**历史**，和若干个仍在不同噪声水平上、正在被并行去噪的**在飞 chunk**。一个 chunk 走到噪声为零就被提交，之后永不重算。cache 满了就 FIFO 淘汰最老的非 sink 帧，剩下的 key 按淘汰帧数整体反向旋转，使位置重新从 0 开始编号，从而始终待在训练见过的 RoPE 范围内；第一个 chunk 作为 sink anchor 永久保留、不参与旋转。这样活动窗口的计算量是固定的，视频长度可以无限延伸。

这里有一个本阶段最重要、也最反直觉的工程发现：**同时在飞的 chunk 数（pipeline width）和每个 chunk 得到的去噪更新次数（ar_step）是两件事，而它们的效应符号相反**。两者相乘等于"有效步数"，所以很容易被合称为"深度"——但实测把在飞 chunk 数从 3 提到 8，同一个权重的 FVD 从 274.8 崩到 713.2，还引入随时间累积的全局外观漂移（逐帧亮度误差斜率从 +1.0 变成 −3.4/100 帧，饱和度误差增速快 20–60 倍）；而在固定宽度下把更新次数从 6 提到 12 反而更好（296.0 → 283.5）。机制上讲得通：在飞的 chunk 越多，一个 chunk 提交时所注意到的邻居里"仍然很噪"的就越多，它是在对一批噪声邻居做条件预测。本项目此前所有 DROID 数字都取自 `inflight=8`（那是某个配置文件的默认值），这一个设置就足以让一个模型看起来差 2.6 倍，并且连锁地让好几个方法结论反号。

训练数据方面，DROID 用的是 LeRobot 格式的 DreamZero 发布版，盘上 13,998 个 episode，取 `exterior_image_1_left` 单视角，全部 resize 到 240×320。这里有一个**数据侧的硬约束**值得单独说：由于 Wan 的 4n+1 时间规则，一个 64 latent-frame 的窗口需要 253 个 RGB 帧，而 DROID 只有 **5,020 / 13,998（36%）** 的 episode 够长——也就是说，决定长时程质量的最后一段课程，只能训在语料有偏的三分之一上。125 帧窗口的可用率是 83%，45 帧是 100%。与此相关的还有一个刚被修掉的设置错误：数据加载器的窗口采样默认是 `frozen`，即**每个 episode 在整个训练过程中只用同一个窗口**。这个默认值是为受控消融设的（两臂必须看到逐字节相同的数据），但对以质量为目的的训练是错的——按当前预算算，stage-1a 是同一个窗口重复 183 次，stage-2b 是 5,020 个固定窗口各重复 89 次。已改为 `epoch` 模式，每轮重新抽窗口，183 次重复变成 183 个不同窗口，零额外算力。三条规模链已因此从零重启。

其余训练设置：优化器 Muon，stage-1 学习率 1e-4、stage-2 降到 2e-5 并带 warmup，梯度裁剪 1.0，EMA 0.9999，bf16 混合精度，50 个离散时间步 bin，logit-normal 时间步采样（均值 0、标准差 1）。全局 batch 在 stage-1 是 64、stage-2 是 8，这个数字在代码里是 fail-closed 校验的：world size × per-rank batch × grad accumulation 必须严格等于配置里声明的值，否则直接中止——因为"静默地按另一个 batch 训练"正是会让整组对比作废的那类错误。

整个流程可以这样看：

```
                              ┌─────────────────────────────────────────┐
   原始视频 + 控制信号          │  Wan2.2 VAE (frozen)                    │
   ─────────────────────────▶ │  时间 4× · 空间 16× · 48 通道            │
   240×320 RGB               │  逐通道 (z − mean) / std 归一化           │
   动作 7-D / 文本            └──────────────────┬──────────────────────┘
                                                 │  latent 15×20×48
                                                 ▼
   ┌──────────────────────────────────────────────────────────────────┐
   │  训练：Block-Causal DiT + Rectified Flow + Diffusion Forcing      │
   │                                                                  │
   │   chunk 切分（2 latent frame = 8 RGB frame）                      │
   │        ↓                                                         │
   │   每个 chunk 独立采样噪声水平，且沿时间非降序                        │
   │   ├── 标准配方：在所有合法非降序序列上均匀采样                        │
   │   └── 本项目 C1：向推理实际走的阶梯对齐（schedule-aligned）          │
   │        ↓                                                         │
   │   条件注入                                                        │
   │   ├── 动作 → MLP → 加进 timestep 流                               │
   │   ├── 动作 → MLP → 零初始化 AdaLN 残差（每 block 低秩）             │
   │   └── 文本 → cross-attention（通用视频域）                         │
   │        ↓                                                         │
   │   本项目 C2：以概率把已提交历史当干净上下文喂回（committed-history） │
   │        ↓                                                         │
   │   flow matching 损失（只在预测 chunk 上，历史 chunk 只作条件）       │
   └──────────────────────────────┬───────────────────────────────────┘
                                  │
                     四段窗口课程（每段接上一段权重）
      21 帧 ──→ 45 帧 ──────────→ 125 帧 ──────────→ 253 帧
      lat 6     lat 12            lat 32             lat 64
      40k 步    84k 步            56k 步             56k 步
      batch 64  batch 64          batch 8            batch 8
      lr 1e-4   lr 1e-4           lr 2e-5            lr 2e-5
                                  │
                                  ▼
   ┌──────────────────────────────────────────────────────────────────┐
   │  流式推理：Rolling KV Cache + 异步去噪阶梯                          │
   │                                                                  │
   │   [ sink anchor ][ 已提交历史 (FIFO) ][ 在飞 chunk × N ]           │
   │         永久          淘汰后整体反向旋转      各处不同噪声水平        │
   │                                                                  │
   │   一个 chunk 噪声归零 → 提交 K/V → 永不重算 → 解码出 RGB            │
   │                                                                  │
   │   ⚠ N（pipeline width）与 ar_step（更新次数）效应符号相反            │
   │     实测最优：N=3 · ar_step=4 · guidance=1.5                       │
   └──────────────────────────────┬───────────────────────────────────┘
                                  ▼
                    评测：绝对指标 + do-nothing 地板 + 自编码器上限
             PSNR / SSIM / LPIPS / FID / FVD · 动作依赖探针 · 响应度 b
```

现在说已经产出的结果。**最硬的一组是外部并读**：在 DriftWorld 协议下（IRASim 官方验证 split，与训练零重叠，8 帧动作条件续写，512 集，同一套 I3D 权重与预处理约定——同名的 FVD 在不同实现间可以差 3–4 倍，所以这一条很重要），Bridge-V2 我们是 **FVD 45.08**，对读 IRASim 的 91.6 和 DriftWorld 的 101.2，冻结帧地板是 238.7；Language-Table 我们是 **FVD 37.97**，对读 IRASim 60.05、DriftWorld 39.09，冻结地板 167.3。RT-1 是唯一的负格：训练终点 30k 步是 79.47，而 DriftWorld 是 68.72；这条链的 FVD 随训练**单调上升**（10k 69.32 → 20k 76.32 → 30k 79.47）而 LPIPS 持平，所以不是预算不足而是训练后期的分布层面退化，10k 处与对手相差 0.9%。我们仍然报训练终点的数字而不是事后挑最优点，并把整条曲线一起列出。Bridge 也有同向现象（58k 40.42 → 100k 45.08），但按区间宽度归一后 RT-1 是每万步 +7.3%、Bridge 是 +2.7%，**同向但速率差 2.7 倍，不能当作同一现象**；四条链的观测窗口起点相差很大（LIBERO 从 20k 起、Bridge 从 58k 起），因此不在可比训练阶段，我们只报链内相对变化，不跨链比 FVD 绝对值。

**第二组是因果性本身的证据**，这是项目的核心论点。五条臂全部从随机初始化、同一批 clip、各训 7500 步、同一评测协议：双向骨干加 diffusion forcing 是 FVD 4454.4、响应度 b = −0.006（即输出运动量与场景实际运动量**零相关**）；同一双向骨干换成同质噪声是 1566.3、b = +0.392；换成 block-causal 之后，chunk1 是 699.6 / b=0.456，chunk4 是 852.1 / b=0.892；再加上预训练初始化是 498.8 / b=1.031。也就是说，**在双向骨干上直接用 diffusion forcing 会让模型比不用它还差 2.8 倍，而换成因果注意力后同配方同协议好 2–6 倍**。因果性不是与 diffusion forcing 并列的一项选择，它是 DF 可用的**前提条件**。这里的 b 是一个我们自建的仪器：它测的是"场景动得多 10 倍时，模型是否也多给约 10 倍运动"的 log-log 斜率，1 为忠实、0 为完全不跟随。之所以需要它，是因为常见的 dynamic degree 之类的比值指标把"整体运动水平"和"是否跟随场景"混在一起，而这两者是可以分别移动的。

**第三组是 DROID 长时程的完整消融**，全部在 253 帧、24 集、协议逐项一致的条件下：训练课程沿 stage-1（45 帧窗口）→ stage-2a（125 帧）→ stage-2b（253 帧）给出 FVD **513.9 → 350.5 → 274.8**，单调改善 47%，而 PSNR 基本持平——课程买到的是分布层面的长时程一致性，不是逐帧锐度。采样器方面，同一权重把在飞 chunk 数从 3 提到 8 是 **274.8 → 713.2**（2.6 倍）；引导强度扫了五档，**1.5 是真正的内部极小值**（1.0→317.2、1.25→289.8、**1.5→274.8**、2→283.5、3→337.8），而此前全项目默认取 2 且从未在机器人域扫过；永久 sink anchor 帧数扫了 1/2/4，给出 283.5/284.7/323.5，**更宽的锚点买不到东西**，所以长时程漂移不是"模型失去初始条件"那类失效，这个假设就地关闭。方法消融方面，关掉我们自己的两项训练侧改动后是 **274.8 → 629.9**（2.3 倍）。

**第四组是评测的天花板与地板**，这是保证前面所有数字可解释的基础设施。DROID 上把真值视频经同一个 VAE 编码再解码、然后当作生成结果打分，21 帧处是 36.21 dB / LPIPS 0.0077，253 帧处是 35.38 dB / 0.0095——**只掉 0.83 dB**，说明自编码器的损失是空间性的、不随时长累积，因此长时程伪影**不是编解码造成的**。我们目前最好的 DROID 模型是 19.71 dB / 0.1337，也就是**只用掉了 56% 的可用余量，LPIPS 差 14 倍**。地板方面，我们对三个公开 benchmark 做过 do-nothing 与 random-motion 双地板检验，结论是 WorldScore 的三个动态维度、VBench 的全部九个维度**没有一个是可采纳的**——其中四个被"冻结帧"最大化（分数 1.0000/1.0002/0.9983/1.0000），五个被"随机打乱帧序"打平，`overall_consistency` 在帧序打乱前后甚至逐位相同，而 `temporal_style` 与它的源码差异只有一行注释、20 个格子的数值完全一致。这条纪律现在是项目的硬规则：**一个指标只有在"什么都不做"和"乱动"都拿不到好分数时，才配承载运动正确性的主张**。

**第五组是系统能力**：真机域生产配置下流式吞吐 48.6 fps，即 3.24 倍实时；首帧延迟 188 ms，而双向模型是 5.6 秒；在线重规划省算 36–74% 且输出与全量重算逐位相同（16/16）；动作到画面结构的延迟地板是 533 ms，恰好等于一次因果提交的物理时间粒度，实验证实它同时也是质量最优点。

**第六组是通用视频域的诊断**，也是当前最清楚的一个负面结构性结论。这个域的响应度 b 只有 **0.29–0.47**，而同一架构在动作条件的机器人域能到 **0.89–1.03**。引导对它完全无效（cfg 2/4/7 给出 b = 0.288/0.269/0.366，无趋势），两条损失侧的干预路线也都按预注册判据关闭了——时间方差匹配（C11）此前关闭，速度重分配（E22a）这次给出 Δb = −0.388，方向与要求相反且 FVD/LPIPS 同时变差。唯一有效的是推理侧的去噪更新次数：从 4 提到 8 让 b 从 0.288 抬到 0.466（1.6 倍）而整体运动量不变，代价 0.65 dB。把这些放在一起，结论是**模型缺的是"该动多快"这个信号本身，而不是缺一个惩罚项**：动作条件明确指定了机械臂该移动多少，而文本不指定运动幅度——"一个人骑自行车"没有说骑多快，所以文本条件模型对运动尺度没有可用信号，只能回归语料均值，于是慢场景多给、快场景少给。修法在**条件侧**。

其他域的当前数字：LIBERO 在 49 帧 24 集协议下是 PSNR 33.99 / LPIPS 0.0190 / FVD 56.5（把每 chunk 更新次数从 2 提到 8 带来的改善是 64.3 → 56.5，同时 PSNR 从 31.50 涨到 33.99）；Language-Table 在 41 帧协议下是 168.0，且扫描确认现状即最优；Bridge-V2 的展示片长度已从 9 帧提到 **77 帧**——9 帧是外部并读协议的长度，用作 benchmark 行正确、用作展示荒谬，而 77 帧是**数据集的硬上限**（Bridge 最长的 held-out episode 只有 79 帧，再长就没有真值可对），同时也是模型 25 帧训练窗口的 3 倍，属于滚动 cache 的真实长时程外推，实测 PSNR 22.58 / LPIPS 0.0748 / FVD 251.7，首帧 37.08 衰减到末帧 20.26。

现在说正在跑的任务。**主线是 0.5B / 1B / 3B 三档规模的从零训练**，这是用户明确要求的架构与规模探索。三条阶梯**除 `model.preset` 一个字段外逐字段相同**：同样的四段窗口课程、同样的步数（40k/84k/56k/56k，合计 236k）、同样的全局 batch、同样的学习率与噪声调度、同样的动作接口、同样的语料、同样的 `epoch` 窗口采样，**全部从随机初始化，没有任何一条从任何权重继续训练**。0.5B 是 28 层 / 宽 1152 / 18 头，1B 是 28 层 / 宽 1536 / 12 头，3B 是 32 层 / 宽 2560 / 20 头。按实测吞吐推算，0.5B 约 559 GPU 时、1B 约 994 GPU 时、3B 约 3156 GPU 时，合计约 **4709 GPU 时**。0.5B 与 1B 各用 4 卡、3B 用 8 卡（相应把梯度累积从 2 降到 1 以保持同一个全局 batch，这一点由 fail-closed 校验保证）。这是 **iso-step 而非 iso-FLOP** 的设计，它回答的是"固定数据预算下容量是否有用"，**不是**"每 GPU 时哪个模型最好"，两个问题不能混为一谈。

这三条链在本报告写作时刚刚因为窗口采样的修正而从零重启，因此实际进度是 **0 / 40000 步**，三个作业都在排队等 GPU（0.5B 与 1B 各等 4 卡，3B 的 smoke 等 8 卡）。按实测速率 0.5B 是 0.47 it/s、1B 是 0.39 it/s，一旦拿到卡，stage-1a 的 40k 步需要 **0.5B 约 24 小时、1B 约 28 小时**；3B 从未训过，必须先通过一个 fail-closed 的 smoke（同时验两端显存与吞吐，OOM、SM 利用率低于 60%、显存低于 12 GB 任一都不放行）才允许起链，按算力比推算 3B 的 stage-1a 约 45 小时（8 卡）。三条链在 40k 步处设有**证据门控**：以同一工作点打分后才提交剩余约 80% 的预算，**在匹配步数下不明显更好的臂就地截断**，不跑到底。全部走完的话，0.5B 约 6 天、1B 约 10 天、3B 约 16 天（各自并行）。

预期结果是这样的。首先，**规模应当在长时程一致性上体现得比在逐帧锐度上更明显**——因为我们已经观察到课程（本质上也是"看更长的上下文"）改善的是 FVD 而不是 PSNR，而容量应当作用在同一个维度上。所以我们预期 FVD 随规模显著下降，PSNR 改善温和。其次，**动作依赖度应当随规模与训练量上升**：目前我们的 DROID 模型 gap/true 只有 7.2%，而固定机位的 LIBERO/LangTable 能到 95–110%，DROID 因为是手持机位、背景与光照独立于动作变化，天花板本来就低（一个训练充分的 DROID 模型实测是 24.1%），所以我们预期 1B/3B 能把 7.2% 推向 20% 上下。第三，**自编码器余量应当被显著吃掉**：现在只用了 56%，如果规模研究把 PSNR 从 19.7 推到 24 以上、LPIPS 从 0.134 压到 0.06 以下，那就是把余量用到 68% 以上，同时长时程伪影应当明显减轻。第四，我们预期**在 40k 步的门控处就能看出三档的排序**，如果 3B 在匹配步数下不明显优于 1B，那本身就是一个值得报告的负结果——它会说明在这个数据规模下容量不是瓶颈。

除主线外还有几个正在跑或待跑的诊断：`e3_chf_onset`（committed-history forcing 的起效时机，已跑 18 小时）与 `e6_data_mix`（数据配比与能力前沿的关系，已跑 7 小时）是路线图上的机制实验；通用视频的条件侧改动（给模型一个显式的运动幅度信号）是上面那个负面结论指向的下一步，尚未实现。

最后是几条本阶段用真金白银换来的方法学纪律，它们已经写进项目文档并且改变了后续所有实验的做法。**第一，一个指标只有在 do-nothing 和 random-motion 双地板下都拿不到好分数时才可用**——我们因此判定 VBench 九个维度全部不可采纳。**第二，FVD 只在 clip 数相同、片长相同的条件下可比**，跨协议的绝对值并列是无意义的，页面上因此把并读协议的数字与展示协议的数字严格分开。**第三，共享参数在臂间不一致时，不能只更正标签，必须重跑受影响的每一格**——我们曾经只改了标签，结果错误的数字继续以"已标注更正"的形式被引用了一整轮。**第四，在坏工作点上做的对照，其差值幅度不可信**——同一个方法消融在错误采样设置下测出 13%、在正确设置下测出 122%，方向对了但幅度差一个数量级。**第五，"广谱均匀"的误差不是没有结构**，它可能是一个作用于整帧的全局缺陷的签名；我们曾把区域分解报出的"均匀"当成噪声地板，直到把四个生成器并排看才发现那是全局外观漂移。**第六，看片能定失效的形态，定不了失效的分布；指标反过来对细结构性失真不敏感——两者都要，不能互相替代。**
