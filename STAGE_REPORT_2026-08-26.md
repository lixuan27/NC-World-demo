# NC-World 阶段报告（2026-08-26）

> 面向第一次接触本项目的读者。目标：用领域通用语言讲清楚我们在做什么、怎么做、
> 已经测出了什么、正在跑什么。**所有数字都是实测**，出处见
> `docs/PAPER_ROADMAP_2026-08-25.md` 的执行日志（§7.x）。

---

## 一、这个项目在做什么

NC-World 在探索一个问题：**如何从随机初始化（from scratch）预训练一个
native causal video world model**——一个按时间顺序、一块一块（chunk-by-chunk）
生成未来画面的视频世界模型，它天生支持流式生成（streaming）、动作条件
（action conditioning）与规划（planning），而不是像主流视频扩散模型那样
一次性双向（bidirectional）生成整段视频再剪开用。

论文定位是**探索性 recipe paper**：把设计空间拆成一系列可控实验（controlled
experiments），每个实验只动一个变量，回答"哪个设计选择买到了什么能力"。
风格参考 *Beyond Language Modeling* 与 *Towards Physics of Multimodal
Pretraining* 的组织方式（仅参考写法；内容全部来自本项目自己的实验）。
MiniWorld 是本组提前放出的论文，其全部资产（架构、受控双向对照、系统能力面板）
直接作为本项目的基座部分，不做对比。

**中心论点（当前版本，随证据更新）**：

> 从零训练原生因果世界模型是可行且值得的——因果注意力是让 diffusion forcing
> 可用的前提（从零对从零，因果比双向好 2–6× FVD）；而"世界模型感"里最难的
> 一块（输出的运动量是否跟随世界实际的运动，我们称 **responsiveness**）
> 不是比值指标能看见的，必须用本项目自造的两把非比值尺子来测。

---

## 二、方法与算法（自然语言版）

**架构**：0.5B 参数的 block-causal DiT（depth 28, width 1152）。视频先经
**冻结的 Wan2.2 VAE** 压缩到潜空间（时间 4×、空间 16×、48 通道），DiT 在潜空间
上工作。注意力掩码是**块因果**的：每个 latent chunk（默认 2 个潜帧 ≈ 8 个
RGB 帧）只能看见自己与过去。文本条件用冻结的 UMT5 编码器；机器人动作按
"每潜帧打包 4 个 RGB 帧的动作"进入 conditioning 流。
（"from scratch" 的严格含义：**因果动力学骨干从随机初始化训练**；VAE 与
文本编码器是冻结的预训练组件。）

**训练目标**：rectified flow（模型预测速度场 v = 数据 − 噪声）+
**diffusion forcing**（同一窗口内不同 chunk 用独立噪声水平，训练时模型就习惯
"过去清晰、未来模糊"的流式状态）。上面再叠三项自研组件：
- **SANS**（schedule-aligned noise sampling）：训练时的噪声水平分布对齐部署
  采样器实际会走的调度；
- **CHF**（committed-history forcing）：以一定概率把模型自己一步生成的结果
  当作历史喂回去，让训练分布靠近部署时"历史是自己生成的"的真实状态；
- **velmatch**（E22a，本周新实现，待训）：按 clip 匹配输出运动量到该 clip
  的真实运动量，且在 batch 内做中心化——全局运动水平按构造不可动，只纠正
  "快场景被压得更狠"的差分收缩。

**推理**：rolling KV cache 流式生成，支持每 chunk 少至 2 次网络前向
（实测真机域 48.6 fps，3.24× 实时；首帧延迟 188 ms）、在线重规划省算
（bit-exact 分叉）、以及 CEM planning。

**全流程框图**：

```
                        ┌────────────────────────────────────────────┐
                        │                训 练 阶 段                   │
  视频数据 ────────────►│  冻结 Wan2.2 VAE ──► 潜空间 (48ch, 4×/16×)  │
  (四机器人域+通用视频)  │        │                                    │
                        │        ▼                                    │
  文本 ──► 冻结 UMT5 ──►│  block-causal DiT (0.5B, 28L)               │
  动作 ──► 4帧/潜帧打包─►│   · rectified flow (预测 v)                 │
                        │   · diffusion forcing (chunk 独立噪声)      │
                        │   · SANS (噪声分布对齐部署调度)              │
                        │   · CHF (自生成历史喂回, p=0.3, 域依赖)      │
                        │   · velmatch (E22a, 中心化运动重分配, 待训)  │
                        └───────────────┬────────────────────────────┘
                                        │ checkpoint（内嵌完整 config=provenance）
                                        ▼
                        ┌────────────────────────────────────────────┐
                        │                推 理 阶 段                   │
                        │  rolling KV cache ──► chunk 逐块流式生成     │
                        │  eff_steps = min(steps, inflight×ar_step)  │
                        │  · 真机 48.6 fps / 首帧 188 ms              │
                        │  · 在线重规划 (bit-exact 分叉, 省算 36-74%) │
                        │  · CEM planning                            │
                        └───────────────┬────────────────────────────┘
                                        ▼
        ┌───────────────────────────────────────────────────────────────┐
        │                     评 测 体 系（本项目独有的部分）              │
        │  L3 同数据并读: Bridge/LangTable/RT-1 (IRASim split, 同度量实现)│
        │  L1 通用: WMB/VBench —— 全部先过两地板法(frozen+shuffled)      │
        │  L5 自造 dynamics 尺子:                                        │
        │    e = 跨速率弹性 (同 clip 放大观测位移, 输出跟不跟)             │
        │    b = 跨场景斜率 (动得多的场景是否得到更多输出运动)             │
        │  纪律: 每条运动结论必须配 Δ水平 / Δ响应度 / Δ保真度 三报         │
        └───────────────────────────────────────────────────────────────┘
```

**训练数据**（全在盘）：
- **机器人域四条线**：DROID（真机，主域，单域做深）、Bridge-V2、
  Language-Table、LIBERO、RT-1（跨本体验证）——动作条件；
- **通用视频线（Track-B）**：OpenVid-HD + Ditto-1M 共 52.5 万 clip
  （实测原生帧率 20–30 fps），文本条件（UMT5 嵌入预计算成 store）；
- 评测另有 IRASim 官方发布的三数据集验证 split（与训练零重叠）。

**关键训练设置**：muon optimizer，bf16，cosine LR（`stop_at_step` 把
"跑多远"与"LR 计划裁在哪"解耦——matched-exposure 对照的必需品），
4×H200/作业，global batch 16–64 视域而定，全部 sbatch 提交并带利用率自查。

---

## 三、已确认的实验结果（全部实测，按主题分组）

### 3.1 基座能力（MiniWorld 部分 + 本项目系统面板）

| 结果 | 数字 |
|---|---|
| 受控双向对照（同代码同数据同 clip，只换采样器）| 因果 PSNR 21.47 / SSIM 0.819 / LPIPS 0.110 / **FVD 229.5** vs 双向 17.73 / 0.644 / 0.240 / 1087 —— 四项全胜 |
| 流式速度 | 真机域 **48.6 fps**（3.24× 实时），首帧 **188 ms**（双向 5.6 s）|
| 长时程 | DROID 253 帧（16.9 s）19.78 dB；LIBERO 253 帧 20.86 dB @57.9 fps |
| 因果独有 | 在线重规划省算 36–74%，输出 bit-exact（16/16）；动作→画面结构延迟地板 533 ms（= 4×2/15 s，被 Δt_c 实验证实恰是质量最优点）|

### 3.2 外部并读（论文最硬的外部数字）

同数据同度量实现（IRASim 官方 split，FVD 同一 I3D 权重——同名 FVD 跨实现可差
3–4 倍），每格附冻结地板：

| FVD ↓ | 冻结地板 | **本模型** | IRASim（发表）| DriftWorld（发表）|
|---|---|---|---|---|
| Bridge-V2 | 238.7 | **40.4** | 91.6 | 101.2 |
| Language-Table（**终版 120k**）| 167.3 | **37.97** | 60.05 | 39.09 |
| RT-1 | 459.5 | 79.5 | 93.0 | **68.7**（我方唯一输格）|

对 IRASim 三战三胜；对 DriftWorld 两胜一负（RT-1 输格如实报告；其收敛探针
正在跑，判"预算问题还是天花板"）。LangTable 已用走满 120k 全预算的终版
checkpoint 重评：三个可排名列同向改善，赢面 2.4%→2.9%。
附带方法学发现：**冻结画面的 SSIM/PSNR 在机器人视频上竞争力十足**（Bridge
地板 SSIM 0.882 > 本模型 0.878，也高于两个发表行）⇒ SSIM/PSNR 是
"静止友好"列，只有 FVD 分辨真承诺。

### 3.3 训练算力 × 推理算力面（Finding IV）

- 加深去噪的 dynamics 代价随预训练成熟**单调收缩 ~6×**（0.070→0.011），
  画质收益每步都在 ⇒ "画质-动态不可兼得"主要是欠训练现象；
- 推理深度有**内部最优**（R4–R8），不随训练移动；
- 按实际前向数排开：**FVD 在 4 次前向饱和**——"教师好 42.8 FVD"全部来自
  guidance，蒸馏问题被改写成"2 前向学生追回 guidance"（已追回 35%）。

### 3.4 时间粒度（H2 结案）

物理时间粒度 Δt_c（每次因果提交覆盖的真实秒数）存在**内部最优 0.533 s**
（FVD 700 / **499** / 852，两端各差 +40%/+71%）；
两条不同路径到达同一 Δt_c 的 FVD 只差 **0.7%** ⇒ Δt_c 是控制质量的变量、
chunk/interval 布局本身不是；但布局控制**运动幅度**（同 Δt_c 两路径 flow
差 70%）——"布局控 dynamics、Δt_c 控质量"两量分离。

### 3.5 Dynamics 的分解：水平 vs 响应度（本周核心产出）

所有比值型 dynamics 指标（flow ratio、dynamic degree）把两件事压成一个数：
**水平**（平均输出多少运动）与**响应度**（输出的量是否跟随这一段世界实际
动了多少）。我们造了两把非比值尺子并用它们得到一串结果：

- **e（跨速率弹性）**：同一批 clip、内容不变、只把观测位移放大 1.72×——
  两个模型的输出只放大 1.01–1.02×（e≈0.02，e=1 被 CI 排除）；
- **b（跨场景斜率）**：动得多 10× 的场景，Track-B 只多给 2.3×（b≈0.37）；
- **数据节奏干预（target_fps）只动水平**（+0.43, t=2.4）**不动响应度**
  （b 0.370→0.383），且**赔保真度**（FVD +75.2）⇒ 原"节奏归一化有效"结论
  按预注册判据撤回——**靠调数据分布"变得更动态"是在调一个不解决
  controllability 的量**；
- **速度线性探针**（两域两模型，闭式岭回归+双对照）：观测速度在**每一层**
  都高度线性可解码（R² 0.86–0.905），而输出不用它 ⇒ **杠杆在目标函数侧**
  （E22a 由此设计）；
- **机制**：低响应度=**差分收缩**（快 clip 压得更狠），且与方向不确定性 ρ
  无关（12 臂实测不负相关）⇒ 不受"幅度干预必伤 MSE"定理保护，E22a 的
  中心化重分配设计因此合法。

### 3.6 从零因果阶梯（本日大更正后的干净版本）

一次 provenance 审计发现 r1_base/sans/both 三臂实为 **warm-start 自
r0_s2_lf16**（CLI override，config 文件里看不见）⇒ 旧"8.9×"数字降级。
**从零对从零的干净对照**（同批 clip）：

| 臂（全部从零，7500 步）| FVD@R8 | 响应度 b |
|---|---|---|
| 因果 chunk1 | 699.6 | +0.456 |
| 因果 chunk4 | 852.1 | **+0.892** |
| 因果 chunk2+interval2 | 857.7 | +0.590 |
| 双向+同质噪声 | 1566.3 | +0.392 |
| 双向+diffusion forcing | 4454.4 | **−0.006** |
| （warm-start 三臂，对照）| 416–499 | 0.97–1.05 |

⇒ **因果性优势 2–6× FVD + 明显更高的 b**（从零对从零成立）；
**双向+DF 的输出运动与场景运动完全无相关**（ρ=−0.004，条件均值的纯粹形态）；
**r0 预训练把 FVD 再压 40%、b 推到 1.0 饱和**（"预训练买响应度"的新证据，H12）；
**chunk 布局调制 b**（c4>fi2>c1，与 FVD 不单调，H13）。

### 3.7 评测完整性（F-V，方法学贡献，三个独立样本）

- **WMB**：冻结首帧得 **9.61/10 > 榜首 KLING 9.10** ⇒ 整张 CVPR'25 榜落在
  do-nothing 包络内（我方 7.32–7.85 的名次同时按此降级，双向折扣）；
- **WorldScore**：三条 dynamics 轴全部不可采（乱动把 magnitude 顶满）；
- **VBench**：**九维零可采**——四维被冻结帧拿满分（1.0000/1.0002/0.9983/1.0000，
  都超过真实视频；"比真实视频还平滑"是症状不是成绩），五维被乱动追平或超过
  （dynamic_degree 0.9950；文本对齐维度对帧序完全不敏感——打乱帧序分数逐位不变）；
  另发现 temporal_style 与 overall_consistency 在 custom-input 模式下按构造是
  同一个量（源码 diff 仅差一行注释）。
- **Physics-IQ 四锚**：GT 100 / 冻结 21.0 / 乱序 1.0 / **本模型 1.06**——
  该轴可采，所以 1.06 是真实且严重的域外物理结果，如实报告并作为下一个靶。

⇒ 合成一般性主张：**现行视频世界模型评测的动态维度，或由不动最大化、或由
乱动饱和**；这正是自造 e/b 尺子的存在理由。

### 3.8 负结果与域依赖（同样入论文）

- **EqF**（去掉噪声水平输入）：训练中程 +0.22 dB 的瞬态优势、6 万步归零；
- **C11**（时间方差匹配）：动了靶、付了 oracle 预测的代价，关闭；
- **CHF 域依赖**：机器人域开启（收益随 horizon 增长，LIBERO 100k 终版
  f49 改善 > f25 再次印证）；通用视频域按可采指标判负（FVD/LPIPS/FID
  三列全差）——原先只有 VBench 证据（不可采），重评后决定碰巧正确；
- **零 shot text-CFG 补救无效**（模型从未训过 text dropout；习得 null 的
  训练侧修复 s6 配对在跑）。

---

## 四、正在跑的任务（2026-08-26 午后，全部实测进度）

| 作业 | 在做什么 | 进度 | 预计还需 |
|---|---|---|---|
| **code_drift_ctl 261038** | 从零训 r1_base 配方 ⇒ 从零阶梯 chunk2 格 + "r0 预训练价值"对照（H12/H13 判决）| 3700/7500 @0.49 it/s | **≈2.2 h** + 打分 40 min |
| **bridge_c3 258971** | Bridge 链最后一段（100k 预算）| 97000/100000 | **≈1.7 h**（随后 c4 无害收尾）|
| **trackb_c4_retry1 261019** | 通用视频主线（150k 预算，OOM 修复后 500G/8worker 稳跑）| 46000/150000 @0.26 it/s | ≈111 h（分段跑，每段 20 h 由滚动维护器接力）|
| **e6_data_mix 259798** | 数据配比 G:A 五臂（100:0→0:100，各 6000 步）——"配比→能力前沿" | 臂 1/5 @4300/6000 | ≈38 h（已挂幂等后继 261220 防墙钟）|
| **c9v3_main_pair 259692** | guidance 内化主对（2 前向学生 vs 4 前向教师）| 1500 步 @0.14 it/s | ≈13 h |
| **s6_tdrop / s6_ctrl** | text-CFG 训练侧修复配对（习得 null 行）| 4950/7500 与 2500/7500 | ≈7 h / ≈15 h |
| **rt1_conv_probe 261247** | RT-1 输格判决：10k/20k 中间点 FVD 斜率（预算问题 vs 天花板）| 生成中 | ≈3 h |
| 排队：E2/E3 retry（CHF 机制/onset）+ 面板、E7（动作进入时机）| 等 GPU 组配额 | — | — |
| 就绪待发：**E22a velmatch**（响应度干预第一臂，λ 门+预注册内置）| 等 261038 落位后提交 | — | 训+评约 6 h |

---

## 五、接下来的计划（按"能否改变某条主张的真假"排序）

1. **261038 落位**（数小时内）→ 从零阶梯 ③ 格补齐 → H12（预训练买响应度）
   与 H13（chunk 布局调制 b）正式化 → **E22a 立即开跑**；
2. **E2/E3**：CHF 是真机制还是历史污染正则、causal curriculum 是否存在（L2 的缺口）；
3. **E6/E7 收官**：数据配比 → 能力前沿、动作进入时机（L4 主体）；
4. **Track-B 150k 主线收官**后：全部 demo 以终版 checkpoint 重制，
   Physics-IQ 以"先越过冻结地板 21 分"为具体靶重测；
5. 论文侧：从零阶梯（同代码同初始化五臂）+ e/b 尺子 + 三 benchmark 地板
   构成三条主线证据链，负结果全部保留。

---

## 六、给读者的三条阅读纪律（本项目用血换来的）

1. **每条运动结论必须同时报 Δ水平 / Δ响应度 / Δ保真度**——只动水平的干预
   不算改善 controllability；
2. **臂间比较核三样：配置、数据、以及 checkpoint 内嵌 config**（provenance
   在产物里，不在 configs/*.json 里——CLI override 会让文件骗人）；
3. **每个 benchmark 先过两地板**（frozen + shuffled）——不可采的轴上，
  任何模型的高分（包括对手的）都不提供信息。
